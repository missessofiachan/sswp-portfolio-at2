## Backend Nice-to-Haves

- Scheduled Cloudinary orphan cleanup  
  - Run a sweep on startup and every 24h to compare stored image URLs against Cloudinary folders and delete orphaned assets left by failed uploads or abandoned drafts.

