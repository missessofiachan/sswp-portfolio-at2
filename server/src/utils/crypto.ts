/**
 * Placeholder module reserved for cryptographic helper utilities (bcrypt/JWT).
 * Exporting an empty object keeps TypeScript aware of module scope even when
 * helpers are not yet implemented.
 */
// bcrypt, jwt helpers
export {};
