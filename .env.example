# shared env hints (never commit real .env)

# SERVER_PORT, JWT_SECRET, FIREBASE_*…# shared examples (do not commit real secrets)
# client uses VITE_ prefix only
VITE_API_URL=http://localhost:4000/api/v1


# server
PORT=4000
JWT_SECRET=change_me
# Option A (recommended for local): point to your service account JSON
# Place the JSON in the repo root or provide an absolute path
# Example (Windows): FIREBASE_CREDENTIALS_FILE=./test1-795d3-firebase-adminsdk-fbsvc-f8f926dfa7.json
FIREBASE_CREDENTIALS_FILE=

# Option B: raw fields (if you cannot use a JSON file)
FIREBASE_PROJECT_ID=
FIREBASE_CLIENT_EMAIL=
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"
